*opencode.txt*             Seamless integration between OpenCode AI and Neovim

==============================================================================
CONTENTS                                                   *opencode-contents*

  1. Introduction .......................... |opencode-introduction|
  2. Installation .......................... |opencode-installation|
  3. Usage ................................ |opencode-usage|
  4. Configuration ........................ |opencode-configuration|
  5. Commands ............................. |opencode-commands|
  6. Mappings ............................. |opencode-mappings|
  7. Contributing ......................... |opencode-contributing|
  8. License .............................. |opencode-license|

==============================================================================
1. INTRODUCTION                                        *opencode-introduction*

OpenCode is a plugin that provides seamless integration between the OpenCode
Code AI assistant (command-line tool) and Neovim. It allows you to:

- Toggle OpenCode in a terminal window at the bottom of your Neovim screen
- Automatically detect and reload files modified by OpenCode
- Keep your Neovim buffers in sync with any changes made by OpenCode

NOTE: This plugin requires the official OpenCode CLI tool to be installed
and available in your system's PATH.

==============================================================================
2. INSTALLATION                                        *opencode-installation*

Install using your favorite plugin manager:

Using lazy.nvim:
>lua
  return {
    "AuenKr/open-code.nvim",
    dependencies = {
      "nvim-lua/plenary.nvim", -- Required for git operations
    },
    config = function()
      require("opencode").setup()
    end
  }
<

Using vim-plug:
>vim
  Plug 'nvim-lua/plenary.nvim'
  Plug 'AuenKr/open-code.nvim'
  " After installing, add this to your init.vim:
  " lua require('opencode').setup()
<

Using packer.nvim:
>lua
  use {
    'AuenKr/open-code.nvim',
    requires = {
      'nvim-lua/plenary.nvim', -- Required for git operations
    },
    config = function()
      require('opencode').setup()
    end
  }
<

PREREQUISITES:
- Neovim 0.7.0 or later
- OpenCode CLI tool (https://github.com/anthropics/opencode)
- plenary.nvim plugin (https://github.com/nvim-lua/plenary.nvim) for git operations

==============================================================================
3. USAGE                                                    *opencode-usage*

- Use the `:OpenCode` command to toggle the OpenCode terminal window
- Press <leader>ac in normal mode to toggle OpenCode terminal window
- Press <C-o> in the terminal to close the OpenCode window
- When OpenCode modifies files that are open in Neovim, they'll be
  automatically reloaded
- When in a git repository, OpenCode will automatically use the git root
  directory as its working directory using pushd/popd commands (configurable)

==============================================================================
4. CONFIGURATION                                    *opencode-configuration*

Configure the plugin by passing a table to the setup function. Below is the
default configuration:

>lua
  require("opencode").setup({
    -- Terminal window settings
    window = {
      split_ratio = 0.3,      -- Percentage of screen for the terminal window (height or width)
      position = "botright",  -- Position of the window: "botright", "topleft", "vertical"/"vsplit", "float", etc.
      enter_insert = true,    -- Whether to enter insert mode when opening OpenCode
      start_in_normal_mode = false, -- Whether to start in normal mode instead of insert mode
      hide_numbers = true,    -- Hide line numbers in the terminal window
      hide_signcolumn = true, -- Hide the sign column in the terminal window
      
      -- Floating window configuration (only applies when position = "float")
      float = {
        width = "80%",        -- Width: number of columns or percentage string
        height = "80%",       -- Height: number of rows or percentage string
        row = "center",       -- Row position: number, "center", or percentage string
        col = "center",       -- Column position: number, "center", or percentage string
        relative = "editor",  -- Relative to: "editor" or "cursor"
        border = "rounded",   -- Border style: "none", "single", "double", "rounded", "solid", "shadow"
      },
    },
    -- File refresh settings
    refresh = {
      enable = true,           -- Enable file change detection
      updatetime = 100,        -- updatetime when OpenCode is active (milliseconds)
      timer_interval = 1000,   -- How often to check for file changes (milliseconds)
      show_notifications = true, -- Show notification when files are reloaded
    },
    -- Git project settings
    git = {
      use_git_root = true,     -- Set CWD to git root when opening OpenCode (if in git project)
    },
    -- Command settings
    command = "opencode",        -- Command used to launch OpenCode (do not include --cwd)
    -- Command variants
    command_variants = {
      -- Conversation management
      continue = "--continue", -- Resume the most recent conversation
      resume = "--resume",     -- Display an interactive conversation picker
      
      -- Output options
      verbose = "--verbose",   -- Enable verbose logging with full turn-by-turn output
    },
    -- Keymaps
    keymaps = {
      toggle = {
        normal = "<C-,>",       -- Normal mode keymap for toggling OpenCode
        terminal = "<C-,>",     -- Terminal mode keymap for toggling OpenCode
        variants = {
          continue = "<leader>cC", -- Normal mode keymap for OpenCode with continue flag
          verbose = "<leader>cV",  -- Normal mode keymap for OpenCode with verbose flag
        },
      }
    }
  })
<

==============================================================================
5. COMMANDS                                                *opencode-commands*

:OpenCode                                                       *:OpenCode*
    Toggle the OpenCode terminal window.

Conversation Management Commands:
:OpenCodeContinue                                           *:OpenCodeContinue*
    Toggle OpenCode with the --continue flag to resume the most recent conversation.

:OpenCodeResume                                             *:OpenCodeResume*
    Toggle OpenCode with the --resume flag to display an interactive conversation picker.

Output Options Commands:
:OpenCodeVerbose                                           *:OpenCodeVerbose*
    Toggle OpenCode with the --verbose flag for full turn-by-turn output.

Note: Commands are automatically generated for each entry in your command_variants configuration.

==============================================================================
6. MAPPINGS                                                *opencode-mappings*

Default key mappings:

  <C-,>        Toggle OpenCode terminal window (both normal and terminal modes)
  
Variant mode mappings (if configured):

  <leader>cC   Toggle OpenCode with --continue flag
  <leader>cV   Toggle OpenCode with --verbose flag

Additionally, when in the OpenCode terminal:

  <C-h>        Move to the window on the left
  <C-j>        Move to the window below
  <C-k>        Move to the window above
  <C-l>        Move to the window on the right
  <C-f>        Scroll full-page down
  <C-b>        Scroll full-page up

Note: After scrolling with <C-f> or <C-b>, you'll need to press the 'i' key
to re-enter insert mode so you can continue typing to OpenCode.

You can customize these mappings in the configuration.

==============================================================================
7. CONTRIBUTING                                          *opencode-contributing*

Contributions to OpenCode are welcome! If you would like to contribute,
please check the CONTRIBUTING.md file in the repository for guidelines:
https://github.com/AuenKr/open-code.nvim/blob/main/CONTRIBUTING.md

==============================================================================
8. LICENSE                                                  *opencode-license*

MIT License

 vim:tw=78:ts=8:ft=help:norl: